---
apiVersion: v1
kind: Service
metadata:
  name: vprodb
  labels:
    app: vpro-db
spec:
  selector:
    app: vpro-db
  ports:
  - port: 3306
    targetPort: vpro-db-port
    protocol: TCP
  type: ClusterIP
