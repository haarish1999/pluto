---
apiVersion: v1
kind: Service
metadata:
  name: vprofile-app-svc
spec:
  selector:
    app: vprofile-app
  ports:
  - port: 8080
    targetPort: vprofile-app-port
    protocol: TCP
  type: ClusterIP
