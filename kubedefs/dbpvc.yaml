---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vprofile-db-pvc
  labels:
    app: vprofile-db
spec:
  resources:
    requests:
      storage: 3Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: default
