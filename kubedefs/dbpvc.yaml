---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vpro-db-pvc
  labels:
    app: vpro-db
spec:
  resources:
    requests:
      storage: 3Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: default
